/* pages/rule/rule.wxss */
.rules {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  height: 100vh;
}

.title {
  font-family: Arial, Helvetica, sans-serif;
  font-size: 48rpx;
  margin-bottom: 20rpx;
}

.content {
  font-family: kaiti;
  font-size: 35rpx;
  margin-bottom: 20rpx;
  text-indent: 2em;
  line-height: 1.5;
}
